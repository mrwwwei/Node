import { BinaryTree } from '../src/binaryTree.js';

describe('Test Binary Tree', () => {
    // 1. init tree before every test

    // 2. test binary tree add a node

    // 3. test binary tree in order travel

    // 4. test binary pre order travel

    // 5. test binary post order travel
})